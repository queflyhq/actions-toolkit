# Reusable pipeline: SvelteKit App CI/CD
# install → build → deploy to Cloudflare Pages
#
# Usage:
#   jobs:
#     ci:
#       uses: queflyhq/actions-toolkit/.github/workflows/sveltekit-app.yml@main
#       with:
#         project: my-website
#       secrets: inherit

name: SvelteKit Pipeline

on:
  workflow_call:
    inputs:
      project:
        description: 'Cloudflare Pages project name'
        required: true
        type: string
      build_command:
        required: false
        type: string
        default: 'npm run build'
      output_dir:
        required: false
        type: string
        default: 'build'
      deploy:
        required: false
        type: boolean
        default: true

jobs:
  build-deploy:
    runs-on: qf-k8s-runner
    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        shell: bash
        run: |
          command -v node >/dev/null 2>&1 || catalyst install node
          echo "Node $(node --version)"

      - name: Configure GitHub Packages
        run: |
          echo "@queflyhq:registry=https://npm.pkg.github.com" > .npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.GIT_BOT_TOKEN }}" >> .npmrc

      - name: Install
        shell: bash
        # esbuild's postinstall occasionally hits ETXTBSY on overlayfs
        # (the binary it just wrote is still being exec'd when npm
        # re-reads it). One retry after a clean reinstall clears it.
        run: |
          attempt() {
            if [ -f package-lock.json ]; then npm ci --no-audit --no-fund
            else npm install --no-audit --no-fund
            fi
          }
          if ! attempt; then
            echo "first install failed — wiping node_modules and retrying"
            rm -rf node_modules
            sleep 3
            attempt
          fi

      - name: Build
        run: ${{ inputs.build_command }}

      - name: Deploy
        if: inputs.deploy && github.ref == 'refs/heads/main'
        uses: queflyhq/actions-toolkit/actions/deploy/cloudflare-deploy@main
        with:
          type: pages
          project: ${{ inputs.project }}
          directory: ${{ inputs.output_dir }}
          account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
